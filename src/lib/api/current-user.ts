/**
 * Placeholder for a real authenticated user id. No auth exists yet — every
 * API route that needs a userId takes it explicitly from the caller (see
 * the Milestone 4.7 decision), so the client needs some consistent value
 * to send until real sessions exist. Replace this with session-derived
 * identity once auth lands.
 */
export const CURRENT_USER_ID = "local-dev-user";
